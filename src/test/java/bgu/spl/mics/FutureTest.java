package bgu.spl.mics;
/*
import static org.junit.Assert.*;


import org.junit.Before;
import org.junit.Test;

import java.util.concurrent.TimeUnit;


public class FutureTest {

    private static Future<Integer> future;
    @Before
    public void setUp() throws Exception{
        future= new Future<Integer>();
    }


    @Test
    public void get() {
        Integer one = 1;
        future.resolve(one);
        assertEquals(one,future.get());
    }

    @Test
    public void resolve() {
        Integer one = 1;
        future.resolve(one);
        assertEquals(one,future.get());
    }

    @Test
    public void isDone() {
        assertFalse(future.isDone());
        Integer one = 1;
        future.resolve(one);
        assertTrue(future.isDone());
    }


    @Test
    public void get2() {
        long secondsToWait = 3;
        TimeUnit units = TimeUnit.SECONDS;
        assertNull(future.get(secondsToWait, units));
        Integer one = 1;
        future.resolve(one);
        assertEquals(one,future.get(secondsToWait,units));
    }
}

*/
